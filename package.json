{
  "name": "reader",
  "private": true,
  "version": "1.0.42",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "firebase deploy --only hosting",
    "sync-sw": "node scripts/sync-version.js",
    "version": "npm run sync-sw && git add public/sw.js",
    "push": "npm version patch && git push origin main --follow-tags && npm run build && npm run deploy"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.4",
    "jszip": "^3.10.1",
    "mammoth": "^1.12.0",
    "pdfjs-dist": "^5.6.205",
    "firebase": "^11.4.0"
  }
}
